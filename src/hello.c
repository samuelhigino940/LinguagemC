// importação da biblioteca padrão para uso de teclado e monitor
#include <stdio.h>

int main() {
 // para exibir uma mensagem na tela iremos usar
 // o comando printf(exibir formatando)
 printf("hello, World!\n");   
// informar ao compilador que o programa 
// acabou
return 0;
}